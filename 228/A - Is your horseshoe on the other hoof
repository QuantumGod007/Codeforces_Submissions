#include <bits/stdc++.h>
using namespace std;
 
void solve() {
    set<int> s;
 
    
    for(int i=0;i<4;i++) {
        int x;
        cin >> x;
        s.insert(x);
        
    }
 
    cout << 4 - s.size() << '
'; 
 
    
}
 
int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    
    
    solve();
    
    return 0;
}