#include <bits/stdc++.h>
using namespace std;
 
int main() {
    int t;
    cin>>t;
    while(t--) {
        string s;
        int n;
        cin >> n;
        cin>>s;
        int i = 0;
        string sl;
        for (int i=0;i<n;i++) {
            s[i] = tolower(s[i]);
 
        }
 
        if(s[i] == 'm')  {
            while(i < n && s[i] == 'm') 
                i++;
        }
        else {
            cout << "NO
";
            continue;
 
        }
 
        if(s[i] == 'e') {
            while(i < n && s[i] == 'e')
                i++;
        }
        else {
            cout << "NO
";
            continue;
        }
        if(s[i] == 'o' ) {
            while(i < n && s[i] == 'o')
                i++;
        }
        else {
            cout << "NO
";
            continue;
        }
 
        if(s[i] == 'w') {
            while(i < n && s[i] == 'w')
                i++;
        }
        else {
            cout << "NO
";
            continue;
        }
        if (i == n) {
            cout << "YES
";
        }
        else {
            cout << "NO
";
        }
 
 
 
    }
}